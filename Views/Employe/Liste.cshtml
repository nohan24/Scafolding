@{
    ViewData["Title"] = "Liste Employe";
}

<h1>Liste Employe</h1>

<table class="table">

    <thead>
        <tr>
            
<th>Nom</th>		
<th>Date_naissance</th>		
 <th>Actions</th> 		
        </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewData["data"] as List<Employe>)
        {
            <tr>
                				
<td>				
@item.Nom
				
</td>				
<td>				
@item.Date_naissance
				
</td>
<td>
		<a asp-action="Update" asp-controller="Employe" asp-route-id="@item.Id">Modifier</a> 
		<a asp-action="Delete" asp-controller="Employe" asp-route-id="@item.Id">Supprimer</a>
</td>

            </tr>
        }
    </tbody>
</table>

<form asp-action="Csv" method="get">
    <button type="submit">Télécharger CSV</button>
</form>

<div>
    <a asp-action="Create">Ajouter un nouveau Employe</a>
</div>

